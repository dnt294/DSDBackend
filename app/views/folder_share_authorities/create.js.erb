<% unless @remote_error %>
    $('#folder-share-authorities').empty().append("<%= j render 'authorities_list', authorities: @folder_share_authorities %>")
<% else %>
    setToast('warning', "<%= @remote_error[:error].to_s %>");
<% end %>